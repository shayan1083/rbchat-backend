version: '3.1'

services:
  db:
    image: postgres:latest
    restart: always
    ports:
      - "5432:5432"
    env_file:
      - ./config/.env
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}